{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "Launch Program",
            "target": "./builddir/pacman",
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "parseText"
        }
    ]
}