#pragma once

enum GhostType {
    BLINKY,
    PINKY,
    INKY,
    CLYDE
};
// The ghost type enum is used to identify the type of ghost in the game.
// Each ghost has its own unique behavior and movement patterns, which are defined in the game logic.